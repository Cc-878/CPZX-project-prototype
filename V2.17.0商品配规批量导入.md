# 商品配规 · 批量导入需求说明

## 一、需求背景

当前「商品配规」的新增、编辑配额页面中，「选择门店」仅支持逐个手动选择门店并单独配置配额。当一次配置涉及的门店数量较多（如几十上百家）时，逐条操作效率低、易出错。

因此，在「选择门店」区域新增 **下载模板** 与 **批量导入** 两个入口，支持通过 Excel/CSV 模板一次性批量录入门店配额，提升配置效率。

## 二、功能范围

| 序号 | 功能点 | 新增页 | 编辑页 |
| --- | --- | --- | --- |
| 1 | 「下载模板」按钮 | ✅ | ✅ |
| 2 | 「批量导入」按钮 | ✅ | ✅ |
| 3 | 模板下载（含字段说明） | ✅ | ✅ |
| 4 | 批量导入 + 校验 | 校验门店是否存在 | 门店不存在则新增、存在则覆盖并校验配额下限 |
| 5 | 导入结果反馈 | ✅ | ✅ |

## 三、页面设计

### 3.1 选择门店区

「选择门店」字段右侧由单个按钮改为三个操作按钮：

```
[ 选择门店 ]  [ ⇓ 下载模板 ]  [ ⇧ 批量导入 ]
```

- **选择门店**：原有门店选择器（逐条选择）。
- **下载模板**：下载标准导入模板文件。
- **批量导入**：打开批量导入弹窗。

编辑页在按钮下方额外展示「当前已选门店」表格，便于对照批量覆盖结果：

| 门店ID | 门店名称 | 小程序配额 | 小程序已用配额 |
| --- | --- | --- | --- |

### 3.2 下载模板

- **文件格式**：CSV（兼容 Excel 打开）。
- **字段定义**：

| 字段 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| 序号 | 是 | 整数 | 从 1 递增，用于定位错误行 |
| 门店ID | 是 | 字符串/整数 | 系统门店唯一标识 |
| 小程序配额 | 是 | 正整数 | 为该门店设置的小程序配额 |

- 模板示例：

```csv
序号,门店ID,小程序配额
1,10001,50
2,10002,30
3,10003,80
```

### 3.3 批量导入弹窗

弹窗交互分三步：

1. **下载模板**：点击「下载模板」获取模板文件。
2. **上传文件**：点击上传区选择文件（支持 `.xlsx` / `.csv`）。
3. **校验结果**：上传后展示逐行校验结果，区分「校验通过 / 校验失败」及失败原因。

校验结果表列：

| 序号 | 门店ID | 小程序配额 | （编辑页多一列：已用配额） | 校验结果 |
| --- | --- | --- | --- | --- |

## 四、校验规则

### 4.1 新增页校验规则

逐行校验导入的门店：

| 情况 | 处理 | 结果提示 |
| --- | --- | --- |
| 门店在系统中存在 | 校验通过 | 「门店存在，校验通过」 |
| 门店在系统中不存在 | 校验失败 | 「门店不存在，校验失败」 |

> 默认策略：失败行跳过、成功行正常导入，并在结果中明确标注。若需「任一失败即整体不导入」，需另行确认。

### 4.2 编辑页校验规则

编辑页批量导入走「新增逻辑 + 覆盖逻辑」混合：

| 情况 | 处理 | 结果提示 |
| --- | --- | --- |
| 门店不存在 | 按新增处理，直接新增 | 「门店不存在，新增」 |
| 门店已存在 | 直接覆盖 | 覆盖前校验配额下限，见下 |
| 门店已存在，导入配额 ≥ 已用配额 | 覆盖通过 | 「门店已存在，覆盖（配额≥已用配额）」 |
| 门店已存在，导入配额 < 已用配额 | 覆盖失败 | 「覆盖失败：配额(X) < 已用配额(Y)」 |

**覆盖硬校验**：导入的「小程序配额」必须 **大于等于** 该门店当前的「小程序已用配额」，否则该行校验失败，不执行覆盖。

## 五、异常处理

| 异常 | 处理 |
| --- | --- |
| 文件格式不支持 | 提示「仅支持 .xlsx / .csv 格式」 |
| 必填字段缺失 | 定位到具体行，提示缺失字段 |
| 小程序配额非正整数 | 定位到具体行，提示「配额需为正整数」 |
| 门店ID 重复 | 提示重复，去重后导入 |

## 六、验收标准

1. 新增页与编辑页「选择门店」区均出现「选择门店 / 下载模板 / 批量导入」三个按钮。
2. 点击「下载模板」能下载包含「序号、门店ID、小程序配额」三列的 CSV 模板。
3. 新增页批量导入：门店不存在时该行校验失败并提示原因。
4. 编辑页批量导入：门店不存在则新增；门店存在则覆盖；覆盖时配额 < 已用配额的行校验失败并提示原因。
5. 导入完成后展示成功/失败明细，失败行不影响成功行导入。
