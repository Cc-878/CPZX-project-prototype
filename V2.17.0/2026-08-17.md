# 2026-08-17

## 工作内容
基于截图 1:1 还原"巢品甄选"PC 管理后台的"商品配规"列表页和"新增配规"表单页。

## 产出文件
- `C:\Users\Lenovo\WorkBuddy\巢品甄选\商品配规.html` — 单文件 HTML 原型，含两个视图：
  - **列表页**：9 行雀巢奶粉数据、共 24 条分页器
  - **新增配规表单页**：基础SKU下拉 + 配额门店单选 + 选择门店 + 批量设置 + 规则生效时间 + 状态开关
  - **页面切换**：点列表"新增"按钮跳表单；表单页"×"或"取消"回列表；顶栏 tab 与底部固定操作条同步显示
  - 使用 multiplatform-prototype-template 技能的 PC admin 风格（深色侧边栏 + 浅色内容区 + 蓝色主按钮）

## 关键技术细节
- 表单 CSS 新增约 50 行（form-row/required/sub-field 缩进/switch 开关/footer-actions）
- 单文件 showPage JS（无外部依赖），tabbar 与 footer-actions 切换可见性
- 字段布局：主字段与子字段分层，子字段 margin-left 缩进 32px 对齐"配额门店"radio 行

## 追加需求：批量导入 + 编辑页（第二轮）
- 新增 MD 需求文档：`商品配规批量导入需求.md`（模板字段序号/门店ID/小程序配额，新增/编辑两套校验规则）
- `商品配规.html` 改造：
  - 表单页由 `#page-add` 升级为通用 `#page-form`，支持新增/编辑两种模式（showPage 传 mode + rowIndex）
  - 选择门店区新增「下载模板」「批量导入」按钮
  - 列表"编辑"链接接通 → 编辑页回填 SKU 名称 + 门店表格（门店ID/名称/配额/已用配额）
  - 批量导入弹窗：上传→校验结果表→开始导入；下载模板用 Blob 生成真实 CSV
  - 差异化校验：新增场景「门店是否存在」；编辑场景「不存在新增 / 存在覆盖 + 配额≥已用配额」